[project]
name = "gtaiv-gtac-patcher"
version = "1.0"
description = "GTA IV Retail Patch Installer for GTA Connected"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv]
dev-dependencies = ["pyinstaller>=6.20.0"]